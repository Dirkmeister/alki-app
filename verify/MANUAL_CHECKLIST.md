# ALKI — Manual Verification Checklist

> Generated by `verify/run.mjs`. These are the things a machine cannot honestly
> sign off — visual rendering, multi-account behaviour, and email round-trips.
> Each is a real check: tick it only after you (or Austin) confirm it on a device.

Tested commit: `851e73266de466abbbfb4dcda2488ef6a8c704dc` (working tree dirty)

## Visual / external (machine cannot judge)

- [ ] **(8096d2f6) Athletic clothing renders correctly across ALL 3 avatar render sites (home hero, switcher, projection)**
      Visually confirm shirt/sports-bra + shorts draw correctly on the 2D SVG AND 3D avatars at every site.
- [ ] **(fa9dcbfe) A 185lb vs a 285lb profile actually look different on the avatar**
      Never code-fixed — was 'verify only'. Create two eidolons (185lb / 285lb, same height) and confirm the bodies read visibly different.
- [ ] **(1.4) Password-reset email round-trip works end-to-end**
      Trigger 'forgot password', receive the email, confirm the link lands on the deployed app (NEXT_PUBLIC_SITE_URL) and lets you set a new password — not a Supabase bypass.

## Flagged MANUAL by the automated run

- [ ] **(3a63b324) Cross-user read isolation (user A cannot see user B rows)** — _Privacy/RLS_
      Requires two authenticated users with logged rows. Policy catalog confirms SELECT is owner-scoped; behavioral proof needs a 2-account round-trip (see MANUAL_CHECKLIST).
- [ ] **(6.7-a) Passing the age gate writes age_verified; profile without it re-gates** — _Compliance_
      Behavioral: requires a signed-in account round-trip (guest mode does not persist). Verify a real account: pass gate → row.age_verified=true; force false → app re-gates on load. See MANUAL_CHECKLIST.
- [ ] **(ef1083fa) Settings HOME and Sign-Out are not adjacent (no accidental sign-out)** — _Navigation (Sprint 4)_
      Sign Out renders only for a SIGNED-IN account (this run is a guest, so no Sign Out exists and no accidental-sign-out risk applies). Verify on a signed-in device that the top-right HOME chrome and the in-card Sign Out are not adjacent. Structurally: HOME is fixed top-right nav chrome; Sign Out lives inside the AccountCard body — different regions.
- [ ] **(3a63b324) User A cannot read User B's progress_logs (cross-user)** — _Privacy/RLS_
      Behavioural cross-user isolation needs two authenticated accounts each with logged rows; standing them up writes to the live project (out of scope for read-only verification). Anon-read PASS + policy-catalog PASS together cover the machine-checkable surface.

## BLOCKED this run (couldn't reach the UI state — re-run or check by hand)

- _(none this run — every check reached its state)_
