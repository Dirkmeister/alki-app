// ── ALKI THEME TOKENS ──────────────────────────────────────
// Single source of truth for the brand palette. Pure constants (no React /
// renderer imports) so they're safe to use from components, screens, and the
// pure lib/ logic alike. Extend deliberately — keep this a flat token set, not
// a styling framework.
export const ACCENT = '#22D68A';
export const BG = '#0a0a0a';
